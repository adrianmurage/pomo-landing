---
---

<section id="get-started" class="get-started">
  <div class="container">
    <div class="content">
      <h2>Ready to Boost Your Productivity?</h2>
      <p>Start using our Pomodoro Timer today and experience the difference in your focus and productivity. No signup required – just click and start your first Pomodoro session.</p>
      
      <div class="cta-form">
        <div class="form-group">
          <button id="start-pomodoro" class="cta-button">
            Start Your First Pomodoro
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10" />
              <line x1="12" y1="6" x2="12" y2="12" />
              <line x1="12" y1="12" x2="16" y2="14" />
            </svg>
          </button>
        </div>
        
        <p class="no-signup">No account required. Free to use.</p>
      </div>
    </div>
    
    <div class="testimonials">
      <div class="testimonial">
        <div class="quote">"This Pomodoro timer has transformed my work habits. I'm more focused and get more done in less time."</div>
        <div class="author">
          <div class="avatar">JD</div>
          <div class="info">
            <div class="name">Jane Doe</div>
            <div class="role">Freelance Writer</div>
          </div>
        </div>
      </div>
      
      <div class="testimonial">
        <div class="quote">"As a student, this tool has been invaluable for my study sessions. I can maintain concentration for longer periods."</div>
        <div class="author">
          <div class="avatar">JS</div>
          <div class="info">
            <div class="name">John Smith</div>
            <div class="role">Computer Science Student</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .get-started {
    padding: 6rem 0;
    background-color: #f5f5f5;
    background-image: linear-gradient(135deg, #f5f5f5 0%, #ffffff 100%);
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .content {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  h2 {
    font-size: 2.5rem;
    line-height: 1.2;
    color: var(--text);
  }

  p {
    font-size: 1.2rem;
    line-height: 1.6;
    color: #555;
  }

  .cta-form {
    margin-top: 1rem;
  }

  .form-group {
    margin-bottom: 1rem;
  }

  .cta-button {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    background-color: var(--accent);
    color: white;
    border: none;
    padding: 1rem 2rem;
    border-radius: 8px;
    font-size: 1.1rem;
    font-weight: 600;
    transition: all 0.2s ease;
  }

  .cta-button:hover {
    background-color: var(--accent-dark);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(255, 99, 71, 0.3);
  }

  .no-signup {
    font-size: 0.95rem;
    color: #777;
    margin-top: 0.75rem;
  }

  .testimonials {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .testimonial {
    background-color: white;
    padding: 1.5rem;
    border-radius: 12px;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);
  }

  .quote {
    font-size: 1.1rem;
    line-height: 1.6;
    color: #444;
    font-style: italic;
    margin-bottom: 1.25rem;
    position: relative;
  }

  .quote::before {
    content: """;
    font-size: 3rem;
    color: #eee;
    position: absolute;
    left: -0.5rem;
    top: -1rem;
    z-index: 0;
    font-family: serif;
  }

  .author {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .avatar {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background-color: var(--accent-light);
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 600;
  }

  .info .name {
    font-weight: 600;
    color: var(--text);
  }

  .info .role {
    font-size: 0.9rem;
    color: #777;
  }

  @media (max-width: 768px) {
    .container {
      grid-template-columns: 1fr;
    }

    .get-started {
      padding: 4rem 0;
    }

    h2 {
      font-size: 2rem;
    }

    p {
      font-size: 1.1rem;
    }
  }
</style>

<script>
  // Simple demo functionality for the Start button
  document.addEventListener('DOMContentLoaded', () => {
    const startButton = document.getElementById('start-pomodoro');
    
    if (startButton) {
      startButton.addEventListener('click', () => {
        alert('Starting your 25-minute Pomodoro session! (In a real app, this would start the timer)');
      });
    }
  });
</script>